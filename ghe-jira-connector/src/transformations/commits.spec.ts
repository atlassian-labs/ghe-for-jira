import { transformCommitsWebhookToRepository } from './commits';
var assert = require('assert');
import 'mocha';

describe('Commits transformer', () => {

    it('should return a repository with a commit', () => {

       // const result = buildRepository(bla bla bla)
       //  assert.equal(result, expected)
    });

});